import styled from 'styled-components';
import theme from '../../styles/theme';

const Error = styled.footer`
    min-height: 86%;
    display: flex;
    align-items: center;
    color: ${theme.colors.worldHover};
`;

export default Error;